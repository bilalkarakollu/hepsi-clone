export interface ProductType {
    id: string;
    title: string;
    price: string;
    description: string;
    category: string;
}

export interface ProductSepetType {
    id: string;
    title: string;
    price: string;
    description: string;
    category: string;
    piece: number;
}